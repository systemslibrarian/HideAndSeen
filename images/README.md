# images

`two_layer_clean.png` and `two_layer_hidden.png` encode the SAME url at the
SAME version. Scan either with a phone: both open

    https://ciphermuseum.com/exhibits/two-layer-qr

`two_layer_hidden.png` additionally carries `MEET AT 7` in its pad codewords.
Regenerate both with `python3 src/hide.py`.
